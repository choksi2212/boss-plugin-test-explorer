name: Release

on:
  workflow_dispatch:
  push:
    branches: [main]

# The release workflow creates a GitHub release and publishes to the BOSS Plugin Store;
# both of those need write access to the repo contents.
permissions:
  contents: write

jobs:
  release:
    uses: risa-labs-inc/BossConsole-Releases/.github/workflows/plugin-release.yml@main
    with:
      boss_plugin_api_version: 'latest'
    secrets:
      BOSS_STORE_PLUGIN_PUBLISH_KEY: ${{ secrets.BOSS_STORE_PLUGIN_PUBLISH_KEY }}
